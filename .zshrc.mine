eval "$(rbenv init -)"

# 秘密鍵ファイル
export KEY_FILENAME='id_dsa'
# パスフレーズを登録すれば自動でssh-addする
export PASSPHRASE=''

export GOROOT=/usr/local/opt/go/libexec
export GOPATH=$HOME

export PATH=$HOME/.evm/bin:$PATH:$HOME/dotfiles/shells:$HOME/bin:$GOROOT/bin:$GOPATH/bin

case "${OSTYPE}" in
darwin*)
    source "$HOME/programs/google-cloud-sdk/path.zsh.inc"
    source "$HOME/programs/google-cloud-sdk/completion.zsh.inc"
#    [ -f ~/dotfiles/.zshrc.osx ] && source ~/dotfiles/.zshrc.osx
#    ;;
# linux*)
#     # ここに設定
#     [ -f ~/dotfiles/.zshrc.linux ] && source ~/dotfiles/.zshrc.linux
     ;;
esac
